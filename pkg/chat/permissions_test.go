package chat_test

import (
	"testing"
)

func TestPermissions_CanUserDoCmd(t *testing.T) {
	cases := []struct {
		Name string
	}{
		{},
	}

	for _, tc := range cases {
		t.Run(tc.Name, func(t *testing.T) {

		})
	}
}
